import React from 'react';

const Search = () => (
  <input
    className="focus-ring"
    type="search"
    placeholder="Search Location"
    name="search"
    autoComplete="on"
    style={{ marginTop: 10 }}
  />

);

export default Search;
